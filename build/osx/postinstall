
echo "Post installation process started"
# Update openssl for the user and not the system
THIS=$(basename $SHELL)
[[ $THIS -eq zsh ]] && echo alias openssl=/usr/local/bin/openssl >> ~/.zshrc
[[ $THIS -eq bash ]] && echo alias openssl=/usr/local/bin/openssl >> ~/.bashrc
echo "Post installation process finished"
