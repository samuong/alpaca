
echo "Post installation process started"
# Post install commands for the user and not the system
THIS=$(basename $SHELL)
# commands for shell env if needed
# [[ $THIS -eq zsh ]] && echo command for zsh shell >> ~/.zshrc
# [[ $THIS -eq bash ]] && echo command for bash shell >> ~/.bashrc
